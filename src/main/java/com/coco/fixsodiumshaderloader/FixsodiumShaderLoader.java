package com.coco.fixsodiumshaderloader;

import net.neoforged.bus.api.IEventBus;
import net.neoforged.fml.common.Mod;
import net.neoforged.fml.ModContainer;

@Mod(FixsodiumShaderLoader.MODID)
public class FixsodiumShaderLoader {
    public static final String MODID = "fixsodiumshaderloader";
    public FixsodiumShaderLoader(IEventBus modEventBus, ModContainer modContainer) {
    }
}
